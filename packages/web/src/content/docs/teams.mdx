---
title: Teams
description: Multi-agent collaboration for complex tasks.
---

Teams let multiple AI specialists collaborate on a single goal. An orchestrator staffs a team, assigns work, triggers peer reviews, and converges on a result — all autonomously.

:::tip
Use teams for complex tasks that benefit from specialized perspectives — architecture decisions, full-feature implementation, or security-sensitive work.
:::

Teams run alongside the regular single-agent mode. Your existing sessions and workflows are unaffected.

---

## Start a session

Run the `/team` command with a description of what you want done.

```txt frame="none"
/team Build a REST API for user management with auth, rate limiting, and tests
```

The orchestrator takes over. It staffs the right specialists, decomposes the work, and drives the team through to completion. If the team gets stuck on something it can't resolve, it prompts you for input.

---

### Understand the execution flow

Here's what happens when you type `/team`:

1. The command system replaces `$ARGUMENTS` with your input and sends it to the LLM as a user message.
2. The LLM sees the instruction to call the `team` tool and invokes it with your goal.
3. The `team` tool calls `Team.start()`, which creates a session, an orchestrator, and a workspace.
4. The orchestrator runs in a loop — staffing agents, assigning tasks, triggering reviews, and advancing phases.
5. If the team needs your input, the tool uses the question system to prompt you directly in the TUI.
6. When the orchestrator signals completion, the tool formats the results and returns them to the LLM.
7. The LLM summarizes the results for you.

---

## Know the architecture

A team session has five layers that work together.

| Layer            | What it does                                                    |
| ---------------- | --------------------------------------------------------------- |
| **Orchestrator** | Drives the loop — staffs agents, assigns tasks, advances phases |
| **Roster**       | Agent lifecycle — spawn, retire, track status and metrics       |
| **Workspace**    | Shared state — goal, plan, decisions, artifacts, questions      |
| **Messages**     | Typed communication graph between agents                        |
| **Reviews**      | Critique → revision loops with auto-escalation                  |

The orchestrator is the only agent that sees the full picture. Individual specialists see a filtered view based on the [sharing strategy](#choose-a-sharing-strategy).

---

### Review the data model

Everything is persisted in SQLite across 5 tables with cascade deletes.

| Table            | Purpose                            | Key columns                                                  |
| ---------------- | ---------------------------------- | ------------------------------------------------------------ |
| `team_session`   | One row per team session           | `goal`, `phase`, `status`, `sharing_strategy`                |
| `workspace`      | Shared state sections (8 per team) | `section`, `content` (JSON), `version`, `last_updated_by`    |
| `agent_instance` | One row per spawned agent          | `role`, `expertise` (JSON), `relationships` (JSON), `status` |
| `team_message`   | Communication graph                | `from_role`, `to_role`, `type`, `content`, `ref_ids` (JSON)  |
| `review_thread`  | Review lifecycle tracking          | `author_role`, `reviewer_role`, `status`, `round`            |

Each table has a foreign key to `team_session` with `ON DELETE CASCADE`. Compound indexes cover the common query patterns. The migration runs automatically on startup.

---

### Know the source layout

```txt frame="none"
src/team/
├── index.ts              Team namespace (create, start, cancel, status)
├── team.sql.ts           Drizzle schema (5 tables)
├── orchestrator.ts       Orchestrator loop (11 actions, phase budgets)
├── execute.ts            Execution bridge (context building, output parsing)
├── roster.ts             Agent lifecycle
├── workspace.ts          Shared state CRUD
├── message.ts            Team message graph (10 types)
├── review.ts             Review thread CRUD
├── review-loop.ts        Critique → revision cycle (max 3 rounds)
├── roles.ts              9 built-in role templates + signal matching
├── hooks.ts              14 bus events for plugins
├── sharing/
│   ├── strategy.ts       SharingStrategy interface + resolve()
│   ├── selective.ts      Default: filtered context per agent
│   ├── hierarchical.ts   Orchestrator-curated context
│   └── broadcast.ts      Full visibility for all agents
└── prompt/               12 prompt template files (.txt)

src/tool/
├── team.ts               TeamTool — wraps Team.start() as LLM-callable tool
└── team.txt              Tool description

src/command/template/
└── team.txt              /team command template ($ARGUMENTS → tool call)
```

---

## Understand the lifecycle

Every team session flows through statuses and phases.

---

### Track the status

| Status         | Meaning                                  |
| -------------- | ---------------------------------------- |
| `active`       | Team is working                          |
| `waiting_user` | Orchestrator escalated a question to you |
| `complete`     | All work is done                         |
| `cancelled`    | Session was cancelled                    |

When the orchestrator uses the `escalate` action, the session flips to `waiting_user`. You'll see a question prompt in the TUI. After you answer, the session returns to `active`.

---

### Move through phases

Every session progresses through five phases. The orchestrator controls transitions.

| Phase            | Step budget | Purpose                                    |
| ---------------- | ----------- | ------------------------------------------ |
| `understanding`  | 10          | Analyze the goal, clarify requirements     |
| `design`         | 15          | Architect the solution, make key decisions |
| `implementation` | 30          | Build it — code, configs, migrations       |
| `verification`   | 15          | Review, test, fix issues                   |
| `complete`       | —           | Wrap up and report results                 |

Budgets are measured in orchestrator steps. If a phase exceeds its budget, the orchestrator auto-advances to the next one. The total step limit across all phases is 100.

---

## Know the roles

Nine built-in role templates are available. The orchestrator picks the right ones based on your goal.

| Role                       | Expertise                                        | Has signals |
| -------------------------- | ------------------------------------------------ | ----------- |
| **Architect**              | System design, patterns, technical decisions     | No          |
| **Developer**              | Implementation, code quality, debugging          | No          |
| **QA**                     | Testing, verification, edge cases                | Yes         |
| **Security reviewer**      | Vulnerabilities, auth patterns, input validation | Yes         |
| **Performance specialist** | Optimization, profiling, scalability             | Yes         |
| **Database specialist**    | Schema design, migrations, query optimization    | Yes         |
| **DevOps**                 | Deployment, CI/CD, infrastructure                | Yes         |
| **UX reviewer**            | Usability, accessibility, WCAG compliance        | Yes         |
| **Documentation writer**   | Technical writing, API docs, user guides         | Yes         |

Each template defines a system prompt, workspace read/write permissions, and expertise tags. Every spawned agent gets its own OpenCode session linked to the `session` table.

---

### Understand signals

Roles with signals are auto-suggested through signal detection. When an agent's output mentions terms like "security", "vulnerability", or "injection", the system suggests spawning a security reviewer.

A role needs at least 2 signal hits to be suggested. Roles without signals — architect and developer — are never auto-suggested. The orchestrator must explicitly staff them.

---

### Register custom roles

You can register your own role templates programmatically.

```ts title="custom-role.ts"
import { Roles } from "./team/roles"

Roles.register({
  role: "api-designer",
  prompt: "You are an API design specialist...",
  description: "REST/GraphQL API design and consistency",
  expertise: ["api design", "REST", "GraphQL", "OpenAPI"],
  workspaceRead: ["goal", "constraints", "plan", "decisions"],
  workspaceWrite: ["artifacts", "questions"],
  signals: ["api", "endpoint", "REST", "GraphQL", "OpenAPI"],
})
```

The `workspaceRead` and `workspaceWrite` arrays control which workspace sections the agent can access. The `signals` array drives auto-suggestion.

---

## Know the actions

The orchestrator drives the team by emitting one JSON action per turn. There are 11 actions.

| Action     | Target field         | Purpose                                             |
| ---------- | -------------------- | --------------------------------------------------- |
| `staff`    | `roles[]`            | Spawn multiple agents and assign initial tasks      |
| `assign`   | `role`               | Give a task to an existing agent                    |
| `spawn`    | `role`               | Add a single new specialist to the roster           |
| `retire`   | `role`               | Remove an agent that's no longer needed             |
| `route`    | `from`, `to`         | Forward a question from one agent to another        |
| `decide`   | —                    | Record an architectural or design decision          |
| `escalate` | —                    | Ask the user a question when the team is stuck      |
| `advance`  | `phase`              | Move to the next phase                              |
| `review`   | `author`, `reviewer` | Trigger a review loop between two agents            |
| `mediate`  | `review_id`          | Resolve an escalated review with a binding decision |
| `complete` | —                    | End the session with a summary                      |

Actions are parsed from JSON, optionally wrapped in a markdown code block. Invalid actions are retried.

---

### Know the composition guidelines

The orchestrator follows sizing heuristics from its system prompt.

- **Simple bug fix**: Developer + QA (2 agents)
- **Feature addition**: Architect + Developer + QA (3 agents)
- **Security-sensitive**: add Security Reviewer
- **Performance-critical**: add Performance Specialist
- **Database changes**: add Database Specialist

The orchestrator starts minimal and spawns more agents as needed.

---

## Understand communication

Agents communicate through typed messages stored in the `team_message` table. There are 10 message types.

| Type            | Purpose                                         |
| --------------- | ----------------------------------------------- |
| `proposal`      | Agent proposes a design or approach             |
| `critique`      | Reviewer critiques an artifact                  |
| `question`      | Agent asks another agent a question             |
| `answer`        | Agent answers a question                        |
| `decision`      | Decision recorded by orchestrator               |
| `handoff`       | Orchestrator assigns work to an agent           |
| `status`        | Status update (phase change, completion, error) |
| `artifact`      | Code, config, or other output                   |
| `spawn_request` | Agent or system requests spawning a specialist  |
| `escalation`    | Question escalated to user or orchestrator      |

Messages can be targeted (agent-to-agent) or broadcast (to the whole team). Each message can carry `ref_ids` linking to review threads and `workspace_mutations` describing state changes.

---

### Know the output tags

Agents use tag-based structured output. The execution layer parses these tags from the LLM response.

| Tag                | Extracted to              | Purpose                         |
| ------------------ | ------------------------- | ------------------------------- |
| `[PROPOSAL]`       | `proposals[]`             | Design or architecture proposal |
| `[QUESTION: role]` | `questions[]`             | Ask a specific agent a question |
| `[CONCERN: topic]` | `concerns[]`              | Flag a concern for the team     |
| `[ARTIFACT]`       | `artifacts[]`             | Code, config, or deliverable    |
| `[SPAWN: role]`    | `spawnRequests[]`         | Request a specialist be added   |
| `[CRITIQUE]`       | `critiques[]`             | Review feedback on an artifact  |
| `[COMPLETE]`       | `complete` flag + summary | Agent signals task is done      |
| `[APPROVED]`       | `approved` flag           | Reviewer approves the artifact  |

Tags are case-insensitive. Each tag's content extends to the next tag or end of text.

---

## Use the workspace

The workspace is shared state organized into 8 sections, initialized when a team session is created.

| Section        | Type   | Initial value       | Content                         |
| -------------- | ------ | ------------------- | ------------------------------- |
| `goal`         | string | User's request      | The original task description   |
| `constraints`  | array  | `[]`                | Boundaries and requirements     |
| `plan`         | object | `{status: "draft"}` | Architecture and task breakdown |
| `tasks`        | array  | `[]`                | Work items with status tracking |
| `decisions`    | array  | `[]`                | Decisions with rationale        |
| `questions`    | array  | `[]`                | Open and answered questions     |
| `artifacts`    | object | `{}`                | Code, configs, and outputs      |
| `agent_states` | object | `{}`                | Per-agent working state         |

Each section is versioned and tracks who last updated it. The workspace supports `get`, `set`, `append`, and `remove` operations.

---

### Track decisions

Decisions have a structured format with rationale and alternatives.

```json
{
  "id": "workspace_01jnb...",
  "description": "Use JWT with httpOnly cookies",
  "rationale": "Stateless, works with CDN, secure against XSS",
  "alternatives": ["Session-based auth", "OAuth tokens in localStorage"],
  "made_by": "architect",
  "status": "approved",
  "timestamp": 1740067200000
}
```

The orchestrator records decisions via the `decide` action. Review approvals also create decision entries.

---

### Route questions

Questions are tracked with routing and status.

```json
{
  "id": "workspace_01jnb...",
  "question": "Should we use separate tables for credentials?",
  "asked_by": "developer",
  "routed_to": "architect",
  "status": "open"
}
```

The orchestrator uses the `route` action to assign a question to the right specialist, then assigns that agent to answer it.

---

## Choose a sharing strategy

Sharing strategies control what each agent sees and how their outputs propagate back to the workspace. The default is `selective`.

---

### Use selective

Each agent sees:

- The full workspace **summary** (a rendered text overview)
- **Detailed content** for sections they have read access to
- **Messages** addressed to them, from them, or broadcast to the team
- **Active reviews** they're involved in
- **All team decisions** (everyone gets these regardless of permissions)

On propagation, the agent's collaborators and reviewers are notified of changes. Write permissions are enforced — agents can only update sections listed in their `workspaceWrite`.

---

### Use hierarchical

Each agent sees:

- A **workspace overview** (summary only)
- **Detailed content** for sections they have read access to
- A **communication notice** telling them to route all questions through the orchestrator

No messages or reviews are injected. The orchestrator curates all information flow. On propagation, only the orchestrator is notified.

This is the most restrictive strategy. Use it for sensitive tasks where you want tight information control.

---

### Use broadcast

Every agent sees:

- The **full workspace** — all 8 sections in detail
- **All recent team messages** (up to 20)
- **All active reviews**
- The **full team roster** with status and expertise

On propagation, all agents except the author are notified. Write permission checks are skipped. Maximum visibility, highest token cost.

---

## Understand reviews

The review protocol ensures quality through structured critique-revision loops.

---

### Know the flow

1. The orchestrator triggers a review by emitting `{"action":"review","artifact":"...","author":"developer","reviewer":"architect"}`.
2. The reviewer receives the artifact plus the reviewer-instructions prompt.
3. The reviewer responds with `[APPROVED]` (done) or `[CRITIQUE]` (needs work).
4. If critiqued, the author receives the critique plus the revision-instructions prompt.
5. The author responds with a `## Revision Summary` and an `[ARTIFACT]` block containing the revised work.
6. Steps 2-5 repeat for up to **3 rounds**.
7. If the review doesn't converge after 3 rounds, it's **auto-escalated** to the orchestrator.

---

### Track review status

| Status           | Meaning                                        |
| ---------------- | ---------------------------------------------- |
| `active`         | Review is in progress                          |
| `approved`       | Reviewer signed off                            |
| `needs_revision` | Critique recorded, waiting for author revision |
| `escalated`      | 3 rounds without convergence, needs mediation  |

The orchestrator can mediate escalated reviews using the `mediate` action, which records a decision and force-approves the thread.

---

### Know the review prompts

The review system uses two dedicated prompt templates.

**`reviewer-instructions.txt`** tells the reviewer to:

- Check the artifact against the team's plan and constraints
- Respond with `[APPROVED]` if quality criteria are met
- Respond with `[CRITIQUE]` with specific, actionable feedback if issues are found
- Be proportional — minor style issues shouldn't block approval

**`revision-instructions.txt`** tells the author to:

- Address every critique point explicitly (fix or disagree with evidence)
- Provide a `## Revision Summary` explaining what was changed
- Wrap the revised work in an `[ARTIFACT]` block
- Never silently ignore feedback

---

## Track agent status

Every agent on the roster has a status that the orchestrator monitors.

| Status    | Meaning                       |
| --------- | ----------------------------- |
| `idle`    | Ready for new work            |
| `working` | Currently executing a task    |
| `waiting` | Blocked on input from another |
| `retired` | Removed from the team         |

Retired agents are excluded from `Roster.list()` and `Roster.get()` lookups. The roster also tracks `steps_used` and `tokens_consumed` per agent.

---

## Listen to events

Teams emit 14 events through the bus system. You can subscribe to these in [plugins](/docs/plugins).

```ts title=".opencode/plugins/team-logger.ts"
import type { Plugin } from "@opencode-ai/plugin"

export const TeamLogger: Plugin = async ({ client }) => {
  return {
    event: async ({ event }) => {
      if (event.type === "team.completed") {
        await client.app.log({
          body: {
            service: "team-logger",
            level: "info",
            message: `Team completed: ${event.properties.summary}`,
          },
        })
      }
    },
  }
}
```

---

### Review the event list

**Session lifecycle:**

- `team.created` — New team session started
- `team.updated` — Session status changed (e.g. `active` → `waiting_user`)
- `team.completed` — Session finished, includes summary string

**Orchestrator:**

- `team.phase_changed` — Phase transition (includes `teamSessionID` and new `phase`)
- `team.decision` — Decision recorded in workspace
- `team.escalated` — Question escalated to user

**Agent lifecycle:**

- `team.agent.spawned` — New agent joined the roster
- `team.agent.retired` — Agent removed from the roster
- `team.agent.updated` — Agent status changed (e.g. `idle` → `working`)

**Communication:**

- `team.message` — Message sent between agents (includes full `Info` object)

**Workspace:**

- `workspace.updated` — Workspace section changed (includes `section` and `updatedBy`)

**Reviews:**

- `team.review.started` — Review thread created
- `team.review.completed` — Review approved
- `team.review.escalated` — Review hit max rounds (includes `reason` string)

---

## Know the prompts

The team module includes 12 prompt templates in `src/team/prompt/`.

| File                         | Used by                 | Purpose                                        |
| ---------------------------- | ----------------------- | ---------------------------------------------- |
| `orchestrator.txt`           | Orchestrator loop       | Team management instructions and action format |
| `architect.txt`              | Architect agent         | System design and pattern selection            |
| `developer.txt`              | Developer agent         | Implementation and code quality                |
| `qa.txt`                     | QA agent                | Testing and verification                       |
| `security-reviewer.txt`      | Security reviewer agent | Vulnerability assessment                       |
| `performance-specialist.txt` | Performance agent       | Optimization and profiling                     |
| `database-specialist.txt`    | Database agent          | Schema design and query optimization           |
| `devops.txt`                 | DevOps agent            | Deployment and infrastructure                  |
| `ux-reviewer.txt`            | UX reviewer agent       | Usability and accessibility                    |
| `documentation-writer.txt`   | Documentation agent     | Technical writing                              |
| `reviewer-instructions.txt`  | Review loop (reviewer)  | How to critique artifacts                      |
| `revision-instructions.txt`  | Review loop (author)    | How to address critique feedback               |

The orchestrator prompt is the most critical — it defines the action format, team composition guidelines, intervention triggers, and phase progression rules.

---

## Use the API

The team feature is primarily accessed via the `/team` command. For programmatic use, the `Team` namespace and `team` tool are also available.

---

### Know the tool

The `team` tool is registered in the tool registry and callable by any LLM agent. It accepts two parameters.

| Parameter          | Type   | Required | Default       | Description                                 |
| ------------------ | ------ | -------- | ------------- | ------------------------------------------- |
| `goal`             | string | Yes      | —             | The task for the team                       |
| `sharing_strategy` | enum   | No       | `"selective"` | `selective`, `hierarchical`, or `broadcast` |

When the team needs user input (via the `escalate` action), the tool uses the question system to prompt you directly in the TUI. The tool returns a formatted summary with the final phase, team roster, and recent activity.

---

### Call start directly

`Team.start()` creates a session and runs the orchestrator loop to completion.

```ts
const result = await Team.start({
  goal: "Build a REST API with auth",
  sharingStrategy: "selective",
  onEscalate: async (question) => {
    return "Use JWT with httpOnly cookies"
  },
  onStatus: (message) => console.log(message),
  abort: controller.signal,
})

// result.teamSession — the final Team.Info
// result.summary — completion summary string
```

The `onEscalate` callback is called when the orchestrator uses the `escalate` action. The session status flips to `waiting_user` until you return an answer.

---

### Inspect a session

`Team.status()` returns the current state of a team session.

```ts
const status = Team.status(teamSessionID)
// status.info — session metadata (phase, status, strategy)
// status.roster — active agents with role, status, expertise
// status.recentActivity — last 10 messages (type, from, to, content)
// status.openQuestions — unanswered questions
// status.workspaceSummary — rendered workspace text
```

---

### Cancel a session

`Team.cancel()` retires all agents and sets the session status to `cancelled`.

```ts
Team.cancel(teamSessionID)
```

---

## Verify the feature

To test the team feature end-to-end, follow these steps.

---

### Restart the server

You need to restart the OpenCode server and TUI client after the code changes. The restart picks up:

- The new `team` tool registration in the tool registry
- The database migration that creates the 5 team tables
- The updated `/team` command template

---

### Run a test prompt

Open a project in the TUI and type:

```txt frame="none"
/team Build a simple utility function that validates email addresses with tests
```

---

### Know what to expect

Watch for these steps in the output:

1. **Tool call** — The LLM should call the `team` tool with your goal
2. **Orchestrator staffing** — The orchestrator should spawn agents (likely Architect + Developer + QA)
3. **Phase progression** — The session should move through understanding → design → implementation → verification → complete
4. **Review cycles** — You may see critique → revision exchanges between agents
5. **Completion** — A summary with the team roster and recent activity

If the team needs your input, you'll see a question prompt in the TUI. Answer it and the team continues.

:::caution
The team session may take several minutes depending on task complexity and model speed. Each agent runs its own LLM calls sequentially through the orchestrator loop.
:::

---

### Check the database

After a session completes, you can verify the data was persisted by looking at the tables:

- `team_session` — One row with your goal, final phase, and status
- `agent_instance` — One row per agent that was spawned
- `team_message` — The full communication history
- `workspace` — 8 rows (one per section) with the final state
- `review_thread` — One row per review that was triggered

---

## Run the tests

The team module has 396 passing tests across 16 files.

```bash
bun test test/team/
```

:::note
One test shows as "fail" on Windows due to a known `EBUSY` file cleanup issue in the test preload. It's unrelated to team functionality.
:::

---

### Know what's covered

| Test file                            | Area                   | What it tests                                                                       |
| ------------------------------------ | ---------------------- | ----------------------------------------------------------------------------------- |
| `schema-comprehensive.test.ts`       | Database schema        | All 5 tables: insert, read back, enum values, FK cascades, JSON columns             |
| `workspace-comprehensive.test.ts`    | Workspace CRUD         | Create, get, set, append, remove, addDecision, addQuestion, answerQuestion, summary |
| `workspace.test.ts`                  | Workspace basics       | Section initialization, version tracking                                            |
| `roster-comprehensive.test.ts`       | Agent lifecycle        | Spawn, retire, setStatus, incrementSteps, shouldRetire, list filtering              |
| `message-comprehensive.test.ts`      | Message routing        | Send, recent, forRole, byType, broadcast vs targeted                                |
| `review-comprehensive.test.ts`       | Review protocol        | Create, approve, escalate, incrementRound, addCritique, addRevision, history        |
| `roles-comprehensive.test.ts`        | Role templates         | All 9 templates, get, list, register, signal matching                               |
| `roles.test.ts`                      | Role matching          | Signal detection, active-role exclusion, scoring                                    |
| `parse-output.test.ts`               | Output parsing         | All 8 tag types, multi-tag extraction, edge cases                                   |
| `execute-comprehensive.test.ts`      | Execution bridge       | buildContext, buildMessage, parseOutput defaults, extractAssistantText              |
| `orchestrator-comprehensive.test.ts` | Orchestrator utilities | parseAction for all 11 types, extractText, forceAdvance, phase order                |
| `review-loop-comprehensive.test.ts`  | Review loop parsing    | extractRevised with `[ARTIFACT]` tags, fallback behavior                            |
| `sharing-comprehensive.test.ts`      | Sharing strategies     | Strategy resolution, interface conformance                                          |
| `hooks-comprehensive.test.ts`        | Plugin events          | All 14 events exported and accessible                                               |
| `team-index-comprehensive.test.ts`   | Team namespace         | Info schema parsing, Status/Phase enums, Event definitions                          |
| `team-db.test.ts`                    | Database integration   | End-to-end DB operations with real SQLite                                           |

---

### Know what's not tested

Functions that call `SessionPrompt.prompt()` can't be unit tested because they need a real LLM provider. These are:

- `Execute.run()` — runs an agent against the LLM
- `Orchestrator.run()` — the main orchestrator loop
- `ReviewLoop.run()` — the critique-revision cycle
- `Team.start()` — creates a session and runs the orchestrator
- `Roster.spawn()` — creates a `Session` (requires DB + project context)

This follows the project's testing philosophy: avoid mocks, test real implementations. These functions need integration tests with a configured provider.

---

### Run a single file

```bash
bun test test/team/parse-output.test.ts
```

All tests use the `Instance.provide({ directory, fn })` pattern for database access, with `test/preload.ts` for isolation. No mocks — tests run against real SQLite with real migrations.
